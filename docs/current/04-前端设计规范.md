# 前端设计规范

## 设计定位

gcoll 是运维型、工程型数采平台。界面强调稳定、清晰、低干扰和高信息密度，默认风格是“简洁边缘桌面工作台”。

界面不要做成营销首页、展示大屏或装饰型后台。首屏直接进入工作台或最近使用的设备页。

## 技术基线

- Vue 3。
- TypeScript。
- Vite。
- Naive UI。
- Pinia。
- Vue Router。
- ECharts 或 uPlot。
- Monaco Editor。
- lucide 图标。

## 视觉原则

- 主色使用天蓝色。
- 支持浅色和黑夜模式。
- 表格、分栏、表单优先；卡片只用于指标摘要、独立对象或空状态。
- 页面说明使用短句，避免长段落。
- 状态使用状态点、短标签和紧凑文本，不用大面积色块。
- 默认紧凑密度，但保留鼠标点击区域。
- 不使用装饰性大图、渐变大横幅、重阴影、复杂插画和卡片墙。

## 颜色 Token

### 浅色模式

| Token | 色值 | 用途 |
| --- | --- | --- |
| `color.primary` | `#0EA5E9` | 主按钮、选中导航 |
| `color.primary.hover` | `#0284C7` | 主按钮悬停 |
| `color.primary.soft` | `#E0F2FE` | 选中背景 |
| `color.bg.page` | `#F6F9FC` | 页面背景 |
| `color.bg.surface` | `#FFFFFF` | 面板背景 |
| `color.bg.subtle` | `#F1F5F9` | 工具栏背景 |
| `color.border` | `#D8E2EC` | 边框 |
| `color.text.primary` | `#0F172A` | 主文本 |
| `color.text.secondary` | `#475569` | 次级文本 |
| `color.text.muted` | `#94A3B8` | 弱文本 |
| `color.success` | `#10B981` | 正常 |
| `color.warning` | `#F59E0B` | 警告 |
| `color.error` | `#EF4444` | 失败 |

### 黑夜模式

| Token | 色值 | 用途 |
| --- | --- | --- |
| `color.primary` | `#38BDF8` | 主按钮、选中导航 |
| `color.primary.hover` | `#7DD3FC` | 主按钮悬停 |
| `color.primary.soft` | `#083344` | 选中背景 |
| `color.bg.page` | `#0B1120` | 页面背景 |
| `color.bg.surface` | `#111827` | 面板背景 |
| `color.bg.subtle` | `#172033` | 工具栏背景 |
| `color.border` | `#243244` | 边框 |
| `color.text.primary` | `#E5EEF8` | 主文本 |
| `color.text.secondary` | `#A8B3C5` | 次级文本 |
| `color.text.muted` | `#64748B` | 弱文本 |
| `color.success` | `#34D399` | 正常 |
| `color.warning` | `#FBBF24` | 警告 |
| `color.error` | `#F87171` | 失败 |

## 布局规则

### 服务端 Web 控制台

- 左侧固定导航宽度 `232px`。
- 顶部工具栏高度 `56px`。
- 主内容内边距 `20px`。
- 内容区不限制最大宽度，适合大屏运维。
- 列表页优先使用表格。
- 页面标题、说明和主操作尽量放在同一行。

### 桌面端

- 基础视口按 `1280x800` 设计。
- 左侧导航宽度 `208px`，支持折叠到 `64px`。
- 顶部状态栏显示本地运行时、数据库、HTTP API、插件进程和网络状态。
- 内容区内边距 `16px`。
- 核心设备页采用“左侧设备列表 + 右侧设备详情 + 底部点位表”。

## 组件规则

| 场景 | 推荐组件 | 要求 |
| --- | --- | --- |
| 导航 | `NMenu` | 扁平列表，选中项使用天蓝色左边线和浅底 |
| 表格 | `NDataTable` | 支持列设置、筛选、排序和批量选择 |
| 表单 | `NForm` | 标签左对齐，复杂配置按分组拆分 |
| 抽屉 | `NDrawer` | 用于设备、点位、插件、任务详情 |
| 弹窗 | `NModal` | 用于确认、导入和危险操作 |
| 标签 | `NTag` | 表示插件类型、状态、权限 |
| 分段控制 | `NSegmented` | 用于模式切换和上报模式 |
| 开关 | `NSwitch` | 用于启用状态和调试模式 |
| 图表 | ECharts/uPlot | 低装饰、高可读、网格线轻 |

图标按钮优先使用 lucide 图标，并提供悬停提示。每个页面最多突出一个主要操作。

## 页面模板

管理页面默认结构：

```text
页面标题区
筛选或工具条
主数据区域
详情抽屉或编辑弹窗
底部批量操作条
```

表格默认能力：

- 列排序。
- 列筛选。
- 关键词搜索。
- 批量选择。
- 列显示设置。
- 刷新。

抽屉默认结构：

```text
顶部：标题 / 状态 / 关闭
主体：标签页或分区表单
底部：保存 / 取消 / 危险操作
```

## 核心页面

### 工作台

展示运行设备、启用点位、运行任务、插件进程、转发速率、实时吞吐、最近告警和插件状态。不要做营销式大屏。

### 设备管理

服务端使用设备表格 + 右侧抽屉。桌面端使用左侧设备列表 + 右侧详情。设备配置页不编辑点位表，点位表放独立标签页或分区。

### 采集任务

展示任务名称、设备、南向插件、点位数、上报模式、状态、采集速率、规则命中率和最后采集时间。操作包括启动、停止、重启、查看日志和编辑。

### 点位缓存

只展示最新点位缓存，不提供历史查询。当前值列使用等宽数字，变化状态使用轻量高亮。

### 规则过滤

使用规则列表、详情抽屉、条件编辑器和测试样本区。后续可加入 Monaco Editor 高级模式。

### 北向转发

展示转发目标、路由规则、插件调用状态和最近错误。不展示目标系统投递成功率，除非北向插件主动上报。

### 插件中心

当前阶段突出本地插件导入、启停、升级、回滚和调试。在线应用商城只作为弱入口。

### 日志诊断

支持来源、级别、插件、设备、任务和时间范围筛选。日志流支持暂停、继续、导出和清空视图。

## 状态表达

| 状态 | 表达 |
| --- | --- |
| 运行中 | 绿色状态点 + `运行中` |
| 已停止 | 灰色状态点 + `已停止` |
| 离线 | 红色状态点 + `离线` |
| 告警 | 黄色状态点 + 告警数量 |
| 调试中 | 天蓝色 `调试中` 标签 |
| 变化上报 | 蓝色弱标签 |
| 立即上报 | 橙色弱标签 |

状态颜色不得作为唯一信息来源，必须配合文字。

## 可用性检查

每个页面实现后必须检查：

- 浅色和黑夜模式文字对比度。
- `1280px` 宽度下表格列不严重挤压。
- 按钮文字不溢出。
- 抽屉底部操作始终可见。
- 日志和表格长文本有省略和详情查看。
- 插件自定义页面能同步主题和权限状态。
